server:
  port: 8080

spring:
  application:
    name: smart-clinic-management-system

  web:
    resources:
      static-locations: classpath:/static/

  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    cache: false
    encoding: UTF-8

  devtools:
    restart:
      enabled: true

  datasource:
    url: jdbc:mysql://mysql:3306/cms?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: clinic_user
    password: clinic_pass
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
    open-in-view: false

  mongodb:
    uri: mongodb://clinic_user:clinic_pass@mongodb:27017/prescriptions?authSource=admin
    auto-index-creation: true

management:
  health:
    db:
      enabled: true
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: health

jwt:
  secret: 36gENWshLqCFHXsxUAaoxALgStEGZ+qSanlfdrt6KzQ=

api:
  path: /api/

logging:
  level:
    root: INFO
    com.rungrukuk: DEBUG